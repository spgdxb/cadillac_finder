# Cadillac Escalade ESV Price Finder

Simple Python script that looks for **new** Cadillac Escalade ESV listings on dealer inventory pages and finds the **lowest price**.

## Setup (local)

1. Create and activate a virtual environment (optional but recommended).
2. Install dependencies:

   ```bash
   pip install -r requirements.txt
